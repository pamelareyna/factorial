## Comandos básicos de git

* `git init`
* `git status`
* `git diff`
* `git add .`
* `git add --all`
* `git commit -m "El mensaje"`
* `git push -u origin master`
* `git push`
* `git pull origin master`
* `git pull`
